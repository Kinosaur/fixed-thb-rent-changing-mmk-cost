# Python / pandas Practice

The next milestone is one notebook that reproduces the monthly utility-cost calculation from `data_public/monthly_housing_costs_public.csv` and validates it against the equivalent SQL query.

Keep the notebook public-safe: do not load receipt images, room details, FX screenshots, or raw FX observations. State assumptions and compare the selected Python result with SQL.
